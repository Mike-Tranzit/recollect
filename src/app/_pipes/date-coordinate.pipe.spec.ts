import { DateCoordinatePipe } from './date-coordinate.pipe';

describe('DateCoordinatePipe', () => {
  it('create an instance', () => {
    const pipe = new DateCoordinatePipe();
    expect(pipe).toBeTruthy();
  });
});
