import { SearchPlatePipe } from './search-plate.pipe';

describe('SearchPlatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPlatePipe();
    expect(pipe).toBeTruthy();
  });
});
